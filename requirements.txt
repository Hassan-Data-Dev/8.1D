# SIT720 8.1D - Sydney Housing Price Prediction and Decision Support System
# Python 3.12. Install with:  python -m pip install -r requirements.txt

# Core data handling
pandas>=2.2
numpy>=1.26

# Modelling
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3

# Visualisation
matplotlib>=3.8
seaborn>=0.13

# Notebook tooling (execution + figure extraction)
nbformat>=5.9
nbconvert>=7.14
ipykernel>=6.29

# Report generation (docx -> pdf is done manually via Word COM)
python-docx>=1.1

# Part 6: web application
streamlit>=1.36

# Data collection and row verification (collect_data.py, backfill.py,
# verify_rows.py). These drive the installed Chrome over CDP rather than
# Playwright's bundled browser, which domain.com.au blocks.
playwright>=1.40

# Optional extras (Part 2 text features)
wordcloud>=1.9
